package com.dosimetros.backend.entity;

// Enum para el campo estado de Dosimetro
// Evita strings hardcodeados en la lógica de negocio
public enum EstadoDosimetro {
    disponible,
    asignado,
    baja
}
